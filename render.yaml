services:
  - type: web
    name: sentinel-security-dashboard
    runtime: node
    buildCommand: npm run build
    startCommand: npm run dashboard
    envVars:
      - key: NODE_ENV
        value: production
